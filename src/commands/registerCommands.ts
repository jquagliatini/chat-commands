import ICommand from "./ICommand";

/**
 * 
 * @param {ICommand[]} commands all the cmomands to use
 * @return {Function} return parseCommandFunction
 */
const registerCommands = (commands: ICommand[]) => (command: string): string => {

    

    return '';
};

export default registerCommands;